# Build output
[Bb]in/
[Oo]bj/

# Release zips built by installer/build-release.ps1
dist/

# Runtime log written by the charge scripts (state before/after every switch)
dell-battery-charge-log.jsonl

# Runtime log of thermal mode switches (BatteryPerformanceHelper.ps1 -> do-cli.exe)
dell-thermal-mode-log.jsonl

# Visual Studio / Rider
.vs/
.idea/
*.user

# Visual reference for the logo (stock icon preview, not a clearly licensed asset)
logo.webp
