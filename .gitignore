# Build output
[Bb]in/
[Oo]bj/

# Log runtime prodotto da Set-DellBatteryChargeProfile.ps1 (stato prima/dopo ogni esecuzione)
dell-battery-charge-log.jsonl

# Log runtime dei cambi di modalità termica (BatteryChargeHelper.ps1 -> do-cli.exe)
dell-thermal-mode-log.jsonl

# Visual Studio / Rider
.vs/
.idea/
*.user

# Riferimento visivo per il logo (anteprima di stock icon, non un asset con licenza chiara)
logo.webp
